<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- View training.order.line View List -->
    <record id="view_training_order_line_list" model="ir.ui.view">
        <field name="name">view.training.order.line.list</field>
        <field name="model">training.order.line</field>
        <field name="arch" type="xml">
            <list editable="bottom">
                <field name="name" />
                <field name="quantity"></field>
                <field name="unit_price"></field>
                <field name="total_price"></field>
            </list>
        </field>
    </record>

    <!-- Action training.order.line -->
    <record id="action_training_order_line" model="ir.actions.act_window">
        <field name="name">Training Order Line</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">training.order.line</field>
        <field name="view_mode">list</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                There is no examples click here to add new Training Order Line.
            </p>
        </field>
    </record>

</odoo>